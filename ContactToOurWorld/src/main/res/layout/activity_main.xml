<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/relativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/metlik"
    tools:context="com.example.slymn54.contacttoourworld.MainActivity">

    <Button
        android:id="@+id/dugmeServis"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="@string/baslat"
        android:onClick="dugmeServisTikla" />
    <LinearLayout
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:paddingTop="3dp"
        android:layout_width="500dp"
        android:layout_centerHorizontal="true"
        android:layout_height="50dp"
        android:id="@+id/linear1"
        android:background="@color/welcome">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:id="@+id/textView"
            android:text="Welcome To Our World"
            android:textSize="15sp"
            android:textColor="@color/welcomeText"
            android:textAlignment="center"
            android:textStyle="italic" />
    </LinearLayout>

    <ImageView
        android:layout_centerHorizontal="true"
        android:layout_width="300dp"
        android:layout_height="250dp"
        android:id="@+id/imageView"
        android:layout_below="@+id/linear1" />
    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/listView"
        android:layout_below="@+id/imageView">
    </ListView>
</RelativeLayout>
